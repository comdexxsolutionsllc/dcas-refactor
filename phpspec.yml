formatter.name: pretty
suites:
    general:
        namespace: DCASDomain
        spec_path: ./app
        spec_prefix: Spec
        src_path: ./app
    module:
        namespace: Modules
        spec_path: ./app
        spec_prefix: Spec
        src_path: ./app